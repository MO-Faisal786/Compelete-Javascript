// Write a program to print  table for given number (8,9,12,15) using for loop
var i;
var numfortable =15;
for (i = 1;i<=10;i++) {
    console.log(`${numfortable} * ${i} = ${numfortable*i}:`);
};