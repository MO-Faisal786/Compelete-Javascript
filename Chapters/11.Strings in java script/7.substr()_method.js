// The substr() method
// that is similer to slice() but the differnce is that the second parameter specifies the length of the extracting part.
var str = 'Apple, Banana, Kivi';
// let res = str.substr(0,5);
let res = str.substr(-4);
console.log(res);